---
scope: Full system — Universal AI Game Video Engine (LLM → Game JSON → Validator → Game Engine → Asset/Audio → Renderer → MP4 + Hermes queue)
purpose: build-substrate
altitude: feature
updated: 2026-09-11T04:09
---

- (constraint) PRD: Universal AI Game Video Engine (final 2026-09-11) — pivot supersede auto-drawing v2; Factory 50 video 15s, 3 mechanics HI_LO/MOST_EXPENSIVE/ONE_AWAY, 7 scenes + 2 Result variants, mock 50 SKU + affiliate_link, CLI+queue headless for Hermes
- (constraint) Inherited lessons from superseded spine architecture-auto-drawing-2026-09-05 (pipes-and-filters, TypeScript Node, Motion Canvas+FFmpeg, piper TTS, worker pool, seed determinism, atomic registry) — superseded but AD patterns reused where applicable
- (decision) Workspace bound: _bmad-output/planning-artifacts/architecture/architecture-auto-drawing-2026-09-11 — old spine 2026-09-05 marked superseded, new spine governs Universal Game Video Engine
- (decision) AD-1 Paradigm: modular monolith (bounded contexts: game/product/scene/audio/render/queue) — prevents filter sprawl, keeps Scene reuse explicit; avoids pipes-and-filters overhead for 7 scenes
- (decision) AD-2 Voice: clone MC Trần Ngọc (MVP) — piper/viPiper offline không clone được, cần voice-cloning engine (local fine-tune vs cloud) — trade-off cost 0đ vs chất lượng/lisence
- (decision) AD-3 Validator: separate filter before Engine — Validator owns schema/game-logic gate, Engine owns answer/timeline calc; affiliate_link/price ownership question raised
- (decision) AD-2 revised: MVP uses free voice (viPiper offline, no clone) — revert from MC Tran Ngoc clone to keep Local-first 0đ; voice-cloning deferred to Phase 2 as optional swap via ITtsEngine adapter
- (decision) AD-3 ownership clarified: price owned by ProductProvider (Validator hard fail if missing), affiliate_link is publishing-only (Validator warning W_AFFILIATE_MISSING, not block) — link only in comment/caption, not burned into MP4
- (decision) AD-4 Scene reuse: 7 scenes shared, but ONE_AWAY needs DigitReveal variant (digit flip) distinct from PriceReveal — Scene interface must support variant param; 6 scenes identical across mechanics
- (decision) AD-5 Queue: file-based queue/*.json only (no SQLite in MVP) — Hermes writes job JSON, Engine polls FS; simple, no DB dep, sufficient for 50-100 jobs/day
- (decision) AD-6 ProductProvider: 50 files products/p001.json (1 file per SKU, JSON schema zod) — Hermes can update single SKU atomically; read via FS + in-memory cache at startup, reload on file change
- (decision) AD-7 Seed determinism: all random (diversification color/tilt/BGM, LLM retry jitter) must use seeded PRNG (seedrandom) seeded by Game seed — bans Math.random(); ensures 100% replay for debug
- (decision) AD-8 Batch: fail-forward — single job failure does not abort batch; log error, mark failed, continue; final batch_report.json {passed, failed, skipped, avg_render_ms}
- (decision) AD-9 Worker pool: bounded pool size = min(CPU-1, 3), RAM ≤4GB guard; Motion Canvas headless → PNG seq → FFmpeg mux per job
- (event) Spine distilled: 10 ADs (modular monolith, TS Node, Validator filter, ownership, ProductProvider 50 files, Scene 7+DigitReveal, Audio viPiper, Render Motion Canvas→FFmpeg, Queue file, Batch pool+fail-forward+seed), Stack, Structural Seed, Capability Map, Deferred 7 items
- (event) Spine finalized: lint PASS, web verify PASS, adversarial 2 holes closed, status draft→final (10 ADs, 250 lines)
